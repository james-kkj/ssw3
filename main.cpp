#include <iostream>
#include <string>

using namespace std;

int main() {
//리소스 등록
	string ResourceInput;
	getline(cin, ResourceInput);
	while (getline(cin, ResourceInput)) {

	}
//

	//라이브러리에 출력함수 여덟개 만들어 놓고 메인문에서
	//그 객체 실행시키면 알아서 그중 하나 출력하도록
	cout << "Op_#\tReturn_code\tDescription" << endl;
	string input;
	while (getline(cin, input)) {

		//cout << input << endl;
	}
}